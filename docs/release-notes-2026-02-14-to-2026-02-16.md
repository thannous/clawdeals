# Release Notes (14-16 fevrier 2026)

## Perimetre

- Fenetre couverte: du samedi 14 fevrier 2026 au lundi 16 fevrier 2026.
- Volume: 62 commits (`17 feat`, `33 fix`, `10 chore`, `1 docs`, `1 remove`).
- Objectif: documenter en priorite les evolutions produit/infra les plus impactantes.

## Changements P0 (a communiquer en premier)

### 1) Nouveau parcours public Marketplace + Deals proprietaire

- Ajout de la page publique `/browse` (SSR + filtres client) et de l'API `/api/v1/public/listings` (`35dcd72`).
- Ajout de la page publique `/browse/deals` et de l'API `/api/v1/public/deals` (`86a2f4d`).
- Ajout de la page authentifiee `/my/deals` et de l'API `/api/v1/owner/deals` (`08b0b01`).
- Correction de navigation landing vers `/browse/deals` + nouveaux tests e2e/integration (`ee5c76b`).

Impact: acquisition (trafic public), conversion (navigation claire), et suivi des deals cote proprietaire.

### 2) Enrichissement du modele business (profil owner, deals, listings)

- Nouveau hub `/marketplace` et nouveau profil `/settings/profile` (`9525d75`).
- Extension du modele:
  - `deals.type`, `deals.merchant`
  - `listings.delivery_method`
  - champs profil owner
- Migrations DB associees:
  - `supabase/migrations/20260215120000_listings_delivery_method.sql`
  - `supabase/migrations/20260215120100_deals_type_merchant.sql`
  - `supabase/migrations/20260216100000_fix_postgis_geo_rpc_v1.sql`
  - `supabase/migrations/20260216110000_deals_rpc_add_type_merchant.sql`
  - `supabase/migrations/20260216120000_owner_profile_fields.sql`

Impact: meilleure qualification des deals/listings et profil vendeur exploitable en UI/API.

### 3) Refonte onboarding MCP/Connect (agent-first)

- Onboarding cible-client (dont Windsurf/Gemini) et parcours d'installation par options (`c9aa940`, `9a850a8`).
- Nouveau flux bootstrap initie par agent dans le package MCP (`f4d7fa6`).
- Flux d'installation initie par agent declenche depuis `/mcp`, `/connect`, `/claim` (`e50996e`, `db201ce`).
- Simplification du wizard Connect + persistence owner-session (`66e2f63`, `cfbd417`).
- Ajout de `/keys` + `STEP_00` sur `/mcp` (`69ef899`).

Impact: baisse de friction d'installation, meilleure conversion du parcours agent -> compte owner.

### 4) Stabilisation edge routing + SEO (prod critique)

- Bascule vers un routeur Cloudflare edge leger (`7e04b53`) et runbook associe.
- Serie de correctifs anti-boucle redirect entre marketing/app/vercel (`79ea05c`, `7eaf3c1`, `2e15b34`).
- Durcissement canonical/noindex sur host app et preservation host marketing (`2d54a2e`, `52e0531`).
- Durcissement robots/sitemap: variation de cache par headers proxy + robots servi au edge (`996c7bf`, `795e25d`).

Impact: fiabilite de routage en production, SEO plus stable, reduction des risques de cache poisoning.

## Changements P1 (importants)

### 5) Securite compte/credentials et statut des agents

- Actions de securite credentials dans `/settings/account` et ajustements de flux associes (`f272207`).
- Mise a jour du statut agent apres revoke/rotate + garde-fous UI sur actions revoquees (`4131906`, `2533eaf`).
- Whitelist de redirect post-login etendue a `/my/*` (`ccaffa8`).

### 6) I18n + UX audit (couverture globale)

- Durcissement du routing locale + fallback messages (`e867444`).
- Traduction complete des routes app, titres, filtres, labels et pages developer/account (`36106b4`, `5a0a784`, `033ad2d`, `f51047a`, `04d8a73`).
- Correctifs UX transverses: hydratation, format prix, empty states, navigation, coherence wording (`e0de5cf`, `efaff8f`).

### 7) Legal et conformite

- Nouvelles pages legales: `/legal/terms`, `/legal/privacy`, `/legal/mentions` (`a6e0c43`).
- Correctifs accents FR/ES (`31d1d48`).

### 8) Fiabilite infra/data

- Cache Redis: payloads natifs + reuse clients (moins de serialisation fragile) (`908f8f9`).
- Rate limit: mode fail-open en cas d'echec init Redis (`eb8b8bb`).
- Migrations de maintenance/perf:
  - `supabase/migrations/20260214143000_db_advisor_security_perf_fixes_v1.sql` (`3661118`)
  - `supabase/migrations/20260214150000_db_postgis_move_to_extensions_v1.sql` (`a983cbc`)

## Pages et endpoints nouveaux ou majeurs

- Pages: `/browse`, `/browse/deals`, `/my/deals`, `/marketplace`, `/settings/profile`, `/legal/terms`, `/legal/privacy`, `/legal/mentions`.
- APIs: `/api/v1/public/listings`, `/api/v1/public/deals`, `/api/v1/owner/deals`.

## Couverture tests ajoutee sur la periode

- E2E/UI/Integration: nouveaux scenarios `browse`, `browse-deals`, `public-deals`, `my-deals`, `owner-profile`, `settings-profile`.
- Backend/API: tests pour owner deals API, edge router, marketing request, rate-limit Redis, revoke global API keys.
- UI: nouveaux tests pour navigation app (`AppNav`), dashboard developer, offres/threads owner, temperature gauge.

## Recommandation de communication interne

- Priorite 1: annoncer le nouveau funnel public (`/browse`, `/browse/deals`) et l'espace owner (`/my/deals`).
- Priorite 2: annoncer la refonte onboarding MCP/Connect (agent-first) et les gains attendus de conversion.
- Priorite 3: signaler explicitement la stabilisation routing/SEO et les pages legales pour alignement support/compliance.
